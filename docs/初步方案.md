一、整体定位（最终定版）
你：资金方 + 执行方（只负责按指令下单，不干预决策）
AI：全职交易员（GPT‑4o，有记忆、动态决策、动态仓位、动态风控）
本金：1000 USDT 全部交给 AI 管理
周期：快波段（1 小时级别内）
品种：BTC/USDT 现货（只做多）
频率：每 5 分钟一次看盘 + 决策
运行：服务器 24h 运行，无显示器、纯 API
二、AI 交易员的能力范围（最终定版）
AI 可以自主决定：
开仓 / 平仓 / 观望
仓位大小（0%–100%，由 AI 动态决定）
入场价格 / 区间
止损位置与大小
止盈目标（多段止盈均可）
移动止损 / 部分止盈
根据结构、量价、趋势、波动率动态调整策略
AI 必须遵守的铁律（写死在提示词里）：
只做现货、只做多、不做空、不杠杆
永远不扛单，触发止损必须平
永远不逆势开仓
盈亏比至少 1:1.5
单日最大总风险不超过本金 3%
连续亏损后自动降仓
三、数据输入方案（最终定版）
1. 第一次启动：完整历史 K 线（给 AI “开局记忆”）
你一次性发给 AI：
1h：最近 30 根
15m：最近 60 根
5m：最近 120 根
每根格式：[timestamp, open, high, low, close, volume]按 从旧到新 排列。
2. 之后每 5 分钟：增量数据（只发最新变化）
每次只发：
当前价格
最新一根 5m K 线
最新一根 15m K 线
最新一根 1h K 线
当前仓位状态（有仓 / 无仓、入场价、持仓量、盈亏）
关键支撑 / 压力位（你标记的前高前低）
3. 输入格式（最终 JSON）
json
{
  "symbol": "BTC/USDT",
  "current_price": 42800.0,
  "latest_klines": {
    "5m": [1738723200000, 42700.0, 42900.0, 42650.0, 42800.0, 12345.0],
    "15m": [1738722300000, 42500.0, 42950.0, 42400.0, 42800.0, 34567.0],
    "1h": [1738720800000, 42200.0, 43000.0, 42100.0, 42800.0, 89012.0]
  },
  "position": {
    "has_position": true,
    "entry_price": 42300.0,
    "size_usdt": 500.0,
    "current_pnl_pct": 1.18
  },
  "key_levels": {
    "supports": [42200.0, 41800.0],
    "resistances": [43500.0, 44200.0]
  },
  "capital": 1000.0,
  "max_daily_risk_pct": 3.0
}
四、记忆与历史方案（最终定版）
你在服务器维护 会话历史（messages 数组）
每次调用 GPT‑4o 都带上 完整历史 + 最新增量
只保留最近 20 轮对话（user + assistant）
更早历史自动丢弃，防止超 token、保证决策聚焦
历史持久化到数据库 / Redis，重启可恢复
五、系统提示词（最终定版，给 GPT‑4o）
plaintext
你是一名专业的加密货币全职交易员，管理用户 1000 USDT 现货资金，只做 BTC/USDT，只做多，不做空、不杠杆。
你的风格：顺势而为、看结构、看量价、看关键位置、动态仓位、动态风控、灵活应变、不预测、只做高概率。

你将收到：
- 最新价格
- 最新 5m/15m/1h K线
- 当前仓位信息
- 关键支撑/压力位
- 总资金与单日最大风险限制

你可以自主决定：
- 开多 / 平仓 / 观望
- 仓位大小（0%–100%）
- 入场限价/区间
- 止损位置与理由
- 多段止盈目标
- 移动止损 / 部分止盈

你必须严格遵守：
1. 永远不逆势开仓。
2. 永远不扛单，触发止损立即平仓。
3. 盈亏比至少 1:1.5。
4. 单日总风险不超过本金 3%。
5. 连续亏损后自动降低仓位。
6. 只在高概率、低风险位置开仓。
7. 输出必须唯一、明确、可直接执行。

输出格式（严格遵守，只输出 JSON，无任何多余内容）：
{
  "d": "long|close|wait",
  "s": 50, // 仓位百分比，仅 long 有效
  "e": 42800.0, // 入场限价
  "sl": 42100.0, // 止损价
  "tp": [44000.0, 44900.0], // 止盈数组，可 1–2 个
  "r": "1h bullish, 5m bounce with volume" // 一句话理由
}

观望时只输出：
{"d":"wait","r":"no high probability setup"}

平仓时只输出：
{"d":"close","r":"structure broken / stop loss hit / take profit hit"}

现在，根据最新数据与历史上下文，做出专业交易员级别的动态决策。
只输出 JSON，不要任何其他内容。
六、AI 输出格式（最终定版，极简、可直接解析）
开仓示例
json
{"d":"long","s":50,"e":42800.0,"sl":42100.0,"tp":[44000.0,44900.0],"r":"1h bullish + 5m bounce"}
平仓示例
json
{"d":"close","r":"structure broken"}
观望示例
json
{"d":"wait","r":"no setup"}
七、成本与收益（最终定版）
1. 成本（AI “工资”）
每天 12 小时运行：144 次调用
单日 API 成本：≈ $2.16
月成本：≈ $64.8
年成本：≈ $788
2. 预期收益（现实可实现区间）
保守：年净赚 1000–2700 USDT
中性：年净赚 2700–5200 USDT
乐观：年净赚 5200–9200 USDT
3. 投入产出比
年净收益 /（本金 + API 成本）≈ 56%–515%
结论：收益远大于成本，长期必赚
八、服务器执行流程（最终定版）
拉取最新 K 线 + 价格
构建增量输入 JSON
加载会话历史（最近 20 轮）
调用 GPT‑4o API，传入历史 + 最新输入
接收并解析 JSON 指令
执行：
long → 按仓位、限价、止损、止盈下单
close → 市价 / 限价平仓
wait → 不操作
将本次 user/assistant 消息加入历史
裁剪历史到 20 轮
保存历史
等待 5 分钟，重复循环
九、最终一句话总结（你要的全部）
你给 AI 1000 USDT，AI 做全职交易员，你只执行指令。
GPT‑4o 有记忆、动态决策、动态仓位、动态风控。
成本可控，收益健康，方案完整、可直接部署、可长期稳定运行。